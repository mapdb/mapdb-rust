// Code generated by mapdb-codegen. DO NOT EDIT.
use crate::bag::f32_hash_bag::F32HashBag;
use std::collections::HashMap;
use std::fmt;

/// Immutable bag (multiset) of `f32` values with occurrence counts.
#[derive(Debug, Clone)]
pub struct ImmutableF32HashBag {
    counts: HashMap<u32, (f32, usize)>,
    size: usize,
}

impl ImmutableF32HashBag {
    pub fn from_mutable(bag: &F32HashBag) -> Self {
        let mut im = ImmutableF32HashBag {
            counts: HashMap::new(),
            size: 0,
        };
        bag.for_each_with_occurrences(|v, c| {
            im.counts.insert(v.to_bits(), (v, c));
            im.size += c;
        });
        im
    }
    pub fn of(values: &[f32]) -> Self {
        let m = F32HashBag::of(values);
        Self::from_mutable(&m)
    }
    pub fn occurrences_of(&self, value: f32) -> usize {
        self.counts.get(&value.to_bits()).map(|e| e.1).unwrap_or(0)
    }
    pub fn contains(&self, value: f32) -> bool {
        self.occurrences_of(value) > 0
    }
    pub fn size(&self) -> usize {
        self.size
    }
    pub fn size_distinct(&self) -> usize {
        self.counts.len()
    }
    pub fn is_empty(&self) -> bool {
        self.size == 0
    }
    /// Iterates over all elements, repeating each value by its count.
    pub fn iter(&self) -> impl Iterator<Item = f32> + '_ {
        self.counts
            .values()
            .flat_map(|&(v, c)| std::iter::repeat_n(v, c))
    }
    pub fn to_mutable(&self) -> F32HashBag {
        let mut m = F32HashBag::new();
        for &(v, c) in self.counts.values() {
            for _ in 0..c {
                m.add(v);
            }
        }
        m
    }
    /// Expands the bag to a `Vec` containing each value repeated by its count.
    pub fn to_vec(&self) -> Vec<f32> {
        let mut out = Vec::with_capacity(self.size);
        for &(v, c) in self.counts.values() {
            for _ in 0..c {
                out.push(v);
            }
        }
        out
    }
}

impl crate::traits::f32_collection::F32Collection for ImmutableF32HashBag {
    fn len(&self) -> usize {
        self.size()
    }
    fn contains(&self, value: f32) -> bool {
        self.contains(value)
    }
    fn iter(&self) -> impl Iterator<Item = f32> + '_ {
        self.iter()
    }
}

impl crate::traits::f32_collection::F32Bag for ImmutableF32HashBag {
    fn occurrences_of(&self, value: f32) -> usize {
        self.occurrences_of(value)
    }
    fn size_distinct(&self) -> usize {
        self.size_distinct()
    }
}

impl fmt::Display for ImmutableF32HashBag {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        write!(f, "ImmutableBag(size={})", self.size)
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn test_occurrences() {
        let im = ImmutableF32HashBag::of(&[1.0f32, 1.0f32, 2.0f32]);
        assert_eq!(im.occurrences_of(1.0f32), 2);
        assert_eq!(im.size(), 3);
    }
    #[test]
    fn test_to_mutable() {
        let im = ImmutableF32HashBag::of(&[1.0f32]);
        let mut m = im.to_mutable();
        m.add(2.0f32);
        assert_eq!(im.size(), 1);
    }
    #[test]
    fn test_display() {
        assert!(!ImmutableF32HashBag::of(&[1.0f32]).to_string().is_empty());
    }
}
