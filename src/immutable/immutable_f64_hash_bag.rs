// Code generated by mapdb-codegen. DO NOT EDIT.
use crate::bag::f64_hash_bag::F64HashBag;
use std::collections::HashMap;
use std::fmt;

/// Immutable bag (multiset) of `f64` values with occurrence counts.
#[derive(Debug, Clone)]
pub struct ImmutableF64HashBag {
    counts: HashMap<u64, (f64, usize)>,
    size: usize,
}

impl ImmutableF64HashBag {
    pub fn from_mutable(bag: &F64HashBag) -> Self {
        let mut im = ImmutableF64HashBag {
            counts: HashMap::new(),
            size: 0,
        };
        bag.for_each_with_occurrences(|v, c| {
            im.counts.insert(v.to_bits(), (v, c));
            im.size += c;
        });
        im
    }
    pub fn of(values: &[f64]) -> Self {
        let m = F64HashBag::of(values);
        Self::from_mutable(&m)
    }
    pub fn occurrences_of(&self, value: f64) -> usize {
        self.counts.get(&value.to_bits()).map(|e| e.1).unwrap_or(0)
    }
    pub fn contains(&self, value: f64) -> bool {
        self.occurrences_of(value) > 0
    }
    pub fn size(&self) -> usize {
        self.size
    }
    pub fn size_distinct(&self) -> usize {
        self.counts.len()
    }
    pub fn is_empty(&self) -> bool {
        self.size == 0
    }
    /// Iterates over all elements, repeating each value by its count.
    pub fn iter(&self) -> impl Iterator<Item = f64> + '_ {
        self.counts
            .values()
            .flat_map(|&(v, c)| std::iter::repeat_n(v, c))
    }
    pub fn to_mutable(&self) -> F64HashBag {
        let mut m = F64HashBag::new();
        for &(v, c) in self.counts.values() {
            for _ in 0..c {
                m.add(v);
            }
        }
        m
    }
    /// Expands the bag to a `Vec` containing each value repeated by its count.
    pub fn to_vec(&self) -> Vec<f64> {
        let mut out = Vec::with_capacity(self.size);
        for &(v, c) in self.counts.values() {
            for _ in 0..c {
                out.push(v);
            }
        }
        out
    }
}

impl crate::traits::f64_collection::F64Collection for ImmutableF64HashBag {
    fn len(&self) -> usize {
        self.size()
    }
    fn contains(&self, value: f64) -> bool {
        self.contains(value)
    }
    fn iter(&self) -> impl Iterator<Item = f64> + '_ {
        self.iter()
    }
}

impl crate::traits::f64_collection::F64Bag for ImmutableF64HashBag {
    fn occurrences_of(&self, value: f64) -> usize {
        self.occurrences_of(value)
    }
    fn size_distinct(&self) -> usize {
        self.size_distinct()
    }
}

impl fmt::Display for ImmutableF64HashBag {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        write!(f, "ImmutableBag(size={})", self.size)
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn test_occurrences() {
        let im = ImmutableF64HashBag::of(&[1.0f64, 1.0f64, 2.0f64]);
        assert_eq!(im.occurrences_of(1.0f64), 2);
        assert_eq!(im.size(), 3);
    }
    #[test]
    fn test_to_mutable() {
        let im = ImmutableF64HashBag::of(&[1.0f64]);
        let mut m = im.to_mutable();
        m.add(2.0f64);
        assert_eq!(im.size(), 1);
    }
    #[test]
    fn test_display() {
        assert!(!ImmutableF64HashBag::of(&[1.0f64]).to_string().is_empty());
    }
}
