// Code generated by mapdb-codegen. DO NOT EDIT.

/// Read-only trait for any collection of `i32` values.
///
/// Implementors provide three required methods — [`len`], [`contains`],
/// and [`iter`] — and get a rich set of defaulted query methods for free,
/// following the same pattern as Rust's [`Iterator`] trait.
pub trait I32Collection {
    // ── Required methods ────────────────────────────────────────────

    /// Returns the number of elements.
    fn len(&self) -> usize;

    /// Returns true if the collection contains the value.
    fn contains(&self, value: i32) -> bool;

    /// Returns an iterator over the elements.
    fn iter(&self) -> impl Iterator<Item = i32> + '_;

    // ── Defaulted methods ───────────────────────────────────────────

    /// Returns true if the collection is empty.
    fn is_empty(&self) -> bool {
        self.len() == 0
    }

    /// Returns all elements as a Vec.
    fn to_vec(&self) -> Vec<i32> {
        self.iter().collect()
    }

    /// Calls the given function for each element.
    fn for_each(&self, mut f: impl FnMut(i32)) {
        for v in self.iter() {
            f(v);
        }
    }

    /// Returns true if any element satisfies the predicate.
    fn any_satisfy(&self, predicate: impl Fn(i32) -> bool) -> bool {
        self.iter().any(predicate)
    }

    /// Returns true if all elements satisfy the predicate.
    fn all_satisfy(&self, predicate: impl Fn(i32) -> bool) -> bool {
        self.iter().all(predicate)
    }

    /// Returns true if no element satisfies the predicate.
    fn none_satisfy(&self, predicate: impl Fn(i32) -> bool) -> bool {
        !self.iter().any(predicate)
    }

    /// Returns the count of elements satisfying the predicate.
    fn count_where(&self, predicate: impl Fn(i32) -> bool) -> usize {
        self.iter().filter(|&v| predicate(v)).count()
    }

    /// Returns the first element satisfying the predicate, or None.
    fn detect(&self, predicate: impl Fn(i32) -> bool) -> Option<i32> {
        self.iter().find(|&v| predicate(v))
    }

    /// Returns the minimum element, or None if empty.
    fn min_value(&self) -> Option<i32> {
        self.iter().min()
    }

    /// Returns the maximum element, or None if empty.
    fn max_value(&self) -> Option<i32> {
        self.iter().max()
    }

    /// Returns elements satisfying the predicate as a Vec.
    fn select(&self, predicate: impl Fn(i32) -> bool) -> Vec<i32> {
        self.iter().filter(|&v| predicate(v)).collect()
    }

    /// Returns elements NOT satisfying the predicate as a Vec.
    fn reject(&self, predicate: impl Fn(i32) -> bool) -> Vec<i32> {
        self.iter().filter(|&v| !predicate(v)).collect()
    }

    /// Folds all elements using the given function and initial value.
    fn inject_into<R>(&self, initial: R, mut f: impl FnMut(R, i32) -> R) -> R {
        let mut acc = initial;
        for v in self.iter() {
            acc = f(acc, v);
        }
        acc
    }

    /// Returns the sum of all elements.
    fn sum(&self) -> i64 {
        self.iter().map(|v| v as i64).sum()
    }
}

/// Mutable collection trait extending I32Collection.
pub trait I32MutableCollection: I32Collection {
    /// Removes all elements.
    fn clear(&mut self);
}

// ── Category traits — mirror Java's IntList / IntSet / IntBag / IntStack ──
//
// These distinguish *what kind of collection* is required without naming a
// concrete type. `fn process_list<L: I32List>(l: &L)` accepts ArrayList and
// ImmutableArrayList but not HashSet.

/// Read-only ordered list of `i32` values with positional access.
pub trait I32List: I32Collection {
    /// Returns the element at `index`, or None if out of bounds.
    fn get(&self, index: usize) -> Option<i32>;

    /// Returns the index of the first occurrence of `value`, or None if not found.
    fn index_of(&self, value: i32) -> Option<usize>;
}

/// Mutable ordered list extending I32List + I32MutableCollection.
pub trait I32MutableList: I32List + I32MutableCollection {
    /// Appends a value to the end of the list.
    fn push(&mut self, value: i32);
    /// Replaces the element at `index`. Returns the previous value.
    fn set(&mut self, index: usize, value: i32) -> i32;
}

/// Read-only set of `i32` values (no positional access; uniqueness implied).
pub trait I32Set: I32Collection {}

/// Mutable set extending I32Set + I32MutableCollection.
pub trait I32MutableSet: I32Set + I32MutableCollection {
    /// Inserts a value. Returns true if the value was not already present.
    fn add(&mut self, value: i32) -> bool;
}

/// Read-only multiset (bag) of `i32` values with occurrence counts.
pub trait I32Bag: I32Collection {
    /// Returns the number of times `value` occurs in the bag.
    fn occurrences_of(&self, value: i32) -> usize;
    /// Returns the number of *distinct* values (ignoring multiplicity).
    fn size_distinct(&self) -> usize;
}

/// Mutable bag extending I32Bag + I32MutableCollection.
pub trait I32MutableBag: I32Bag + I32MutableCollection {
    /// Adds one occurrence of `value`.
    fn add(&mut self, value: i32);
}

/// Read-only LIFO stack of `i32` values.
pub trait I32Stack: I32Collection {
    /// Returns the top of the stack without removing it.
    fn peek(&self) -> Option<i32>;
}

/// Mutable LIFO stack extending I32Stack + I32MutableCollection.
pub trait I32MutableStack: I32Stack + I32MutableCollection {
    /// Pushes a value onto the top of the stack.
    fn push(&mut self, value: i32);
    /// Pops and returns the top of the stack, or None if empty.
    fn pop(&mut self) -> Option<i32>;
}

#[cfg(test)]
mod verify {
    use super::*;
    fn _assert_collection<T: I32Collection>() {}
    fn _assert_mutable<T: I32MutableCollection>() {}
    fn _assert_list<T: I32List>() {}
    fn _assert_mutable_list<T: I32MutableList>() {}
    fn _assert_set<T: I32Set>() {}
    fn _assert_mutable_set<T: I32MutableSet>() {}
    fn _assert_bag<T: I32Bag>() {}
    fn _assert_mutable_bag<T: I32MutableBag>() {}
    fn _assert_stack<T: I32Stack>() {}
    fn _assert_mutable_stack<T: I32MutableStack>() {}

    /// Compile-time verification that every concrete collection type for
    /// `i32` implements the appropriate read-only and mutable traits.
    /// If any implementation is missing this test fails to compile.
    #[test]
    fn types_implement_traits() {
        // Mutable collections — base Collection / MutableCollection trait
        _assert_collection::<crate::arraylist::i32_array_list::I32ArrayList>();
        _assert_mutable::<crate::arraylist::i32_array_list::I32ArrayList>();
        _assert_collection::<crate::hashset::i32_hash_set::I32HashSet>();
        _assert_mutable::<crate::hashset::i32_hash_set::I32HashSet>();
        _assert_collection::<crate::bag::i32_hash_bag::I32HashBag>();
        _assert_mutable::<crate::bag::i32_hash_bag::I32HashBag>();
        _assert_collection::<crate::bag::i32_tree_bag::I32TreeBag>();
        _assert_mutable::<crate::bag::i32_tree_bag::I32TreeBag>();
        _assert_collection::<crate::stack::i32_array_stack::I32ArrayStack>();
        _assert_mutable::<crate::stack::i32_array_stack::I32ArrayStack>();
        _assert_collection::<crate::treeset::i32_tree_set::I32TreeSet>();
        _assert_mutable::<crate::treeset::i32_tree_set::I32TreeSet>();

        // Immutable collections — read-only trait only
        _assert_collection::<crate::immutable::immutable_i32_array_list::ImmutableI32ArrayList>();
        _assert_collection::<crate::immutable::immutable_i32_hash_set::ImmutableI32HashSet>();
        _assert_collection::<crate::immutable::immutable_i32_hash_bag::ImmutableI32HashBag>();
        _assert_collection::<crate::immutable::immutable_i32_array_stack::ImmutableI32ArrayStack>();

        // Category traits — Lists
        _assert_list::<crate::arraylist::i32_array_list::I32ArrayList>();
        _assert_mutable_list::<crate::arraylist::i32_array_list::I32ArrayList>();
        _assert_list::<crate::immutable::immutable_i32_array_list::ImmutableI32ArrayList>();

        // Category traits — Sets
        _assert_set::<crate::hashset::i32_hash_set::I32HashSet>();
        _assert_mutable_set::<crate::hashset::i32_hash_set::I32HashSet>();
        _assert_set::<crate::treeset::i32_tree_set::I32TreeSet>();
        _assert_mutable_set::<crate::treeset::i32_tree_set::I32TreeSet>();
        _assert_set::<crate::immutable::immutable_i32_hash_set::ImmutableI32HashSet>();

        // Category traits — Bags
        _assert_bag::<crate::bag::i32_hash_bag::I32HashBag>();
        _assert_mutable_bag::<crate::bag::i32_hash_bag::I32HashBag>();
        _assert_bag::<crate::bag::i32_tree_bag::I32TreeBag>();
        _assert_mutable_bag::<crate::bag::i32_tree_bag::I32TreeBag>();
        _assert_bag::<crate::immutable::immutable_i32_hash_bag::ImmutableI32HashBag>();

        // Category traits — Stacks
        _assert_stack::<crate::stack::i32_array_stack::I32ArrayStack>();
        _assert_mutable_stack::<crate::stack::i32_array_stack::I32ArrayStack>();
        _assert_stack::<crate::immutable::immutable_i32_array_stack::ImmutableI32ArrayStack>();

        // Interval — read-only Collection (integer types only)
        _assert_collection::<crate::interval::i32_interval::I32Interval>();
    }

    /// Proves the trait contract is reachable from outside the codegen.
    /// A minimal third-party type implementing I32MutableList, with no
    /// dependency on generated code beyond the trait definitions.
    struct FakeI32List {
        items: Vec<i32>,
    }

    impl I32Collection for FakeI32List {
        fn len(&self) -> usize {
            self.items.len()
        }
        fn contains(&self, value: i32) -> bool {
            self.items.contains(&value)
        }
        fn iter(&self) -> impl Iterator<Item = i32> + '_ {
            self.items.iter().copied()
        }
    }
    impl I32MutableCollection for FakeI32List {
        fn clear(&mut self) {
            self.items.clear();
        }
    }
    impl I32List for FakeI32List {
        fn get(&self, index: usize) -> Option<i32> {
            self.items.get(index).copied()
        }
        fn index_of(&self, value: i32) -> Option<usize> {
            self.items.iter().position(|&v| v == value)
        }
    }
    impl I32MutableList for FakeI32List {
        fn push(&mut self, value: i32) {
            self.items.push(value);
        }
        fn set(&mut self, index: usize, value: i32) -> i32 {
            let old = self.items[index];
            self.items[index] = value;
            old
        }
    }

    #[test]
    fn third_party_impl_satisfies_traits() {
        _assert_mutable_list::<FakeI32List>();
        let mut fake = FakeI32List { items: Vec::new() };
        fake.push(1);
        assert_eq!(fake.len(), 1);
        assert!(fake.contains(1));
        // Verify defaulted methods work via trait dispatch
        assert!(!fake.is_empty());
        assert_eq!(fake.to_vec().len(), 1);
    }
}
