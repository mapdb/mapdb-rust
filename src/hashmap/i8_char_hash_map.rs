// Code generated by mapdb-codegen. DO NOT EDIT.

use crate::hash_table::OpenHashMap;
use std::fmt;

/// Hash map from `i8` keys to `char` values.
/// Open-addressing with linear probing and Robin Hood backward-shift deletion.
#[derive(Debug, Clone)]
pub struct I8CharHashMap {
    inner: OpenHashMap<i8, char>,
}

impl I8CharHashMap {
    pub fn new() -> Self {
        I8CharHashMap {
            inner: OpenHashMap::new(),
        }
    }

    pub fn with_capacity(capacity: usize) -> Self {
        I8CharHashMap {
            inner: OpenHashMap::with_capacity(capacity),
        }
    }

    /// Inserts a key-value pair. Returns the old value if the key was already present.
    pub fn insert(&mut self, key: i8, value: char) -> Option<char> {
        self.inner.insert(key, value)
    }

    /// Returns the value for the key, or None.
    pub fn get(&self, key: i8) -> Option<char> {
        self.inner.get(key)
    }

    /// Returns the value for the key, or the default.
    pub fn get_or_default(&self, key: i8, default: char) -> char {
        self.inner.get(key).unwrap_or(default)
    }

    /// Removes the key. Returns the old value if present.
    pub fn remove(&mut self, key: i8) -> Option<char> {
        self.inner.remove(key)
    }

    pub fn contains_key(&self, key: i8) -> bool {
        self.inner.contains_key(key)
    }

    pub fn contains_value(&self, value: char) -> bool {
        self.inner.iter().any(|(_, v)| v == value)
    }

    pub fn len(&self) -> usize {
        self.inner.len()
    }
    pub fn is_empty(&self) -> bool {
        self.inner.is_empty()
    }
    pub fn clear(&mut self) {
        self.inner.clear();
    }

    /// Ensures the map can accept `additional` more entries without a
    /// rehash. Returns `TryReserveError` on allocator failure. See
    /// `docs/rust/error-handling.md`.
    pub fn try_reserve(
        &mut self,
        additional: usize,
    ) -> Result<(), std::collections::TryReserveError> {
        self.inner.try_reserve(additional)
    }

    /// Iterates over (key, value) pairs.
    pub fn iter(&self) -> impl Iterator<Item = (i8, char)> + '_ {
        self.inner.iter()
    }

    pub fn keys(&self) -> impl Iterator<Item = i8> + '_ {
        self.inner.iter().map(|(k, _)| k)
    }

    pub fn values(&self) -> impl Iterator<Item = char> + '_ {
        self.inner.iter().map(|(_, v)| v)
    }

    pub fn for_each(&self, mut f: impl FnMut(i8, char)) {
        for (k, v) in self.iter() {
            f(k, v);
        }
    }

    pub fn select(&self, predicate: impl Fn(i8, char) -> bool) -> Self {
        let mut result = Self::new();
        for (k, v) in self.iter() {
            if predicate(k, v) {
                result.insert(k, v);
            }
        }
        result
    }

    pub fn reject(&self, predicate: impl Fn(i8, char) -> bool) -> Self {
        let mut result = Self::new();
        for (k, v) in self.iter() {
            if !predicate(k, v) {
                result.insert(k, v);
            }
        }
        result
    }

    pub fn detect(&self, predicate: impl Fn(i8, char) -> bool) -> Option<(i8, char)> {
        self.iter().find(|&(k, v)| predicate(k, v))
    }

    pub fn any_satisfy(&self, predicate: impl Fn(i8, char) -> bool) -> bool {
        self.iter().any(|(k, v)| predicate(k, v))
    }

    pub fn all_satisfy(&self, predicate: impl Fn(i8, char) -> bool) -> bool {
        self.iter().all(|(k, v)| predicate(k, v))
    }

    pub fn none_satisfy(&self, predicate: impl Fn(i8, char) -> bool) -> bool {
        !self.iter().any(|(k, v)| predicate(k, v))
    }

    pub fn count(&self, predicate: impl Fn(i8, char) -> bool) -> usize {
        self.iter().filter(|&(k, v)| predicate(k, v)).count()
    }

    pub fn inject_into<R>(&self, initial: R, mut f: impl FnMut(R, i8, char) -> R) -> R {
        let mut acc = initial;
        for (k, v) in self.iter() {
            acc = f(acc, k, v);
        }
        acc
    }

    pub fn keys_to_vec(&self) -> Vec<i8> {
        self.keys().collect()
    }
    pub fn values_to_vec(&self) -> Vec<char> {
        self.values().collect()
    }

    pub fn with_key_value(mut self, key: i8, value: char) -> Self {
        self.insert(key, value);
        self
    }

    pub fn without_key(mut self, key: i8) -> Self {
        self.remove(key);
        self
    }
}

impl Default for I8CharHashMap {
    fn default() -> Self {
        Self::new()
    }
}

impl PartialEq for I8CharHashMap {
    fn eq(&self, other: &Self) -> bool {
        if self.len() != other.len() {
            return false;
        }
        self.iter()
            .all(|(k, v)| other.get(k).is_some_and(|ov| v == ov))
    }
}

impl fmt::Display for I8CharHashMap {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        write!(f, "{{")?;
        let mut first = true;
        for (k, v) in self.iter() {
            if !first {
                write!(f, ", ")?;
            }
            write!(f, "{}={}", k, v)?;
            first = false;
        }
        write!(f, "}}")
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_insert_get() {
        let mut m = I8CharHashMap::new();
        m.insert(1, 'a');
        m.insert(2, 'b');
        m.insert(3, 'c');
        assert_eq!(m.get(1), Some('a'));
        assert_eq!(m.get(99), None);
        assert_eq!(m.len(), 3);
    }

    #[test]
    fn test_insert_overwrite() {
        let mut m = I8CharHashMap::new();
        m.insert(1, 'a');
        let old = m.insert(1, 'b');
        assert_eq!(old, Some('a'));
        assert_eq!(m.get(1), Some('b'));
    }

    #[test]
    fn test_remove() {
        let mut m = I8CharHashMap::new();
        m.insert(1, 'a');
        m.insert(2, 'b');
        assert_eq!(m.remove(1), Some('a'));
        assert_eq!(m.len(), 1);
        assert!(!m.contains_key(1));
    }

    #[test]
    fn test_contains_key() {
        let mut m = I8CharHashMap::new();
        m.insert(1, 'a');
        assert!(m.contains_key(1));
        assert!(!m.contains_key(99));
    }

    #[test]
    fn test_get_or_default() {
        let mut m = I8CharHashMap::new();
        m.insert(1, 'a');
        assert_eq!(m.get_or_default(1, 'c'), 'a');
        assert_eq!(m.get_or_default(99, 'c'), 'c');
    }

    #[test]
    fn test_clear() {
        let mut m = I8CharHashMap::new();
        m.insert(1, 'a');
        m.clear();
        assert!(m.is_empty());
    }

    #[test]
    fn test_select_reject() {
        let mut m = I8CharHashMap::new();
        m.insert(1, 'a');
        m.insert(2, 'b');
        m.insert(3, 'c');
        assert_eq!(m.select(|_k, v| v > 'a').len(), 2);
        assert_eq!(m.reject(|_k, v| v > 'a').len(), 1);
    }

    #[test]
    fn test_any_all_none() {
        let mut m = I8CharHashMap::new();
        m.insert(1, 'a');
        m.insert(2, 'b');
        assert!(m.any_satisfy(|_k, v| v == 'b'));
        assert!(m.all_satisfy(|_k, v| v > '\0'));
        assert!(m.none_satisfy(|_k, v| v == 'z'));
    }

    #[test]
    fn test_equals() {
        let mut m1 = I8CharHashMap::new();
        m1.insert(1, 'a');
        m1.insert(2, 'b');
        let mut m2 = I8CharHashMap::new();
        m2.insert(2, 'b');
        m2.insert(1, 'a');
        assert_eq!(m1, m2);
    }

    #[test]
    fn test_display() {
        let mut m = I8CharHashMap::new();
        m.insert(1, 'a');
        assert!(!m.to_string().is_empty());
    }

    #[test]
    fn test_resize() {
        let mut m = I8CharHashMap::new();
        for i in 0..100 {
            m.insert(i as i8, char::from(b'A' + (i % 26) as u8));
        }
        assert!(m.len() > 0);
    }

    #[test]
    fn test_try_reserve_happy_path() {
        let mut m = I8CharHashMap::new();
        m.try_reserve(100).unwrap();
    }

    #[test]
    fn test_try_reserve_propagates_capacity_overflow() {
        let mut m = I8CharHashMap::new();
        assert!(m.try_reserve(usize::MAX / 2).is_err());
    }
}

impl crate::traits::i8_char_map::I8CharMap for I8CharHashMap {
    fn len(&self) -> usize {
        self.len()
    }
    fn contains_key(&self, key: i8) -> bool {
        self.contains_key(key)
    }
    fn get(&self, key: i8) -> Option<char> {
        self.get(key)
    }
    fn iter(&self) -> impl Iterator<Item = (i8, char)> + '_ {
        self.iter()
    }
}

impl crate::traits::i8_char_map::I8CharMutableMap for I8CharHashMap {
    fn insert(&mut self, key: i8, value: char) -> Option<char> {
        self.insert(key, value)
    }
    fn remove(&mut self, key: i8) -> Option<char> {
        self.remove(key)
    }
    fn clear(&mut self) {
        self.clear()
    }
}
