// Code generated by mapdb-codegen. DO NOT EDIT.

use std::sync::RwLock;

use super::i8_hash_set::I8HashSet;

/// Thread-safe wrapper around [`I8HashSet`]. Uses an internal `RwLock`
/// so reads may run concurrently while mutations are exclusive.
#[derive(Debug, Default)]
pub struct SynchronizedI8HashSet {
    inner: RwLock<I8HashSet>,
}

impl SynchronizedI8HashSet {
    pub fn new() -> Self {
        SynchronizedI8HashSet {
            inner: RwLock::new(I8HashSet::new()),
        }
    }

    pub fn of(values: &[i8]) -> Self {
        SynchronizedI8HashSet {
            inner: RwLock::new(I8HashSet::of(values)),
        }
    }

    pub fn add(&self, value: i8) -> bool {
        self.inner.write().unwrap().add(value)
    }

    pub fn remove(&self, value: i8) -> bool {
        self.inner.write().unwrap().remove(value)
    }

    pub fn contains(&self, value: i8) -> bool {
        self.inner.read().unwrap().contains(value)
    }

    pub fn len(&self) -> usize {
        self.inner.read().unwrap().len()
    }
    pub fn is_empty(&self) -> bool {
        self.inner.read().unwrap().is_empty()
    }
    pub fn clear(&self) {
        self.inner.write().unwrap().clear();
    }

    pub fn to_vec(&self) -> Vec<i8> {
        self.inner.read().unwrap().to_vec()
    }

    pub fn for_each(&self, f: impl FnMut(i8)) {
        self.inner.read().unwrap().for_each(f);
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use std::sync::Arc;
    use std::thread;

    #[test]
    fn test_basic_ops() {
        let s = SynchronizedI8HashSet::new();
        assert!(s.is_empty());
        assert!(s.add(1));
        assert!(s.add(2));
        assert!(!s.add(1));
        assert!(s.contains(1));
        assert_eq!(s.len(), 2);
        assert!(s.remove(1));
        assert!(!s.contains(1));
        s.clear();
        assert!(s.is_empty());
    }

    #[test]
    fn test_to_vec_and_for_each() {
        let s = SynchronizedI8HashSet::of(&[1, 2]);
        assert!(s.to_vec().len() >= 1);
        let mut count = 0usize;
        s.for_each(|_| count += 1);
        assert!(count >= 1);
    }

    #[test]
    fn test_concurrent() {
        let s = Arc::new(SynchronizedI8HashSet::new());
        let mut handles = Vec::new();
        for _ in 0..4 {
            let c = Arc::clone(&s);
            handles.push(thread::spawn(move || {
                for _ in 0..100 {
                    c.add(1);
                    c.contains(1);
                }
            }));
        }
        for h in handles {
            h.join().unwrap();
        }
        assert!(s.contains(1));
        assert_eq!(s.len(), 1);
    }
}
