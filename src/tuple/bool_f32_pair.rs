// Code generated by mapdb-codegen. DO NOT EDIT.

use std::fmt;

/// Immutable pair of (`bool`, `f32`).
#[derive(Debug, Clone, Copy, PartialEq)]
pub struct BoolF32Pair {
    one: bool,
    two: f32,
}

impl BoolF32Pair {
    pub fn new(one: bool, two: f32) -> Self {
        BoolF32Pair { one, two }
    }
    pub fn one(&self) -> bool {
        self.one
    }
    pub fn two(&self) -> f32 {
        self.two
    }

    pub fn swap(&self) -> F32BoolPair {
        F32BoolPair::new(self.two, self.one)
    }
}

impl fmt::Display for BoolF32Pair {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        write!(f, "({}, {})", self.one, self.two)
    }
}

// Re-export the swapped pair type if it's the same file (self-pair)
use super::f32_bool_pair::F32BoolPair;

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_one_two() {
        let p = BoolF32Pair::new(true, 2.0f32);
        assert_eq!(p.one(), true);
        assert_eq!(p.two(), 2.0f32);
    }

    #[test]
    fn test_equals() {
        let p1 = BoolF32Pair::new(true, 2.0f32);
        let p2 = BoolF32Pair::new(true, 2.0f32);
        let p3 = BoolF32Pair::new(false, 1.0f32);
        assert_eq!(p1, p2);
        assert_ne!(p1, p3);
    }

    #[test]
    fn test_display() {
        let p = BoolF32Pair::new(true, 2.0f32);
        assert!(!p.to_string().is_empty());
    }
}
