// Code generated by mapdb-codegen. DO NOT EDIT.

use crate::hash_table::OpenHashMap;
use std::fmt;

/// Bag (multiset) of `char` values with occurrence counting.
/// Uses custom open-addressing hash table internally.
#[derive(Debug, Clone)]
pub struct CharHashBag {
    counts: OpenHashMap<char, usize>,
    size: usize,
}

impl CharHashBag {
    pub fn new() -> Self {
        CharHashBag {
            counts: OpenHashMap::new(),
            size: 0,
        }
    }

    pub fn of(values: &[char]) -> Self {
        let mut b = Self::new();
        for &v in values {
            b.add(v);
        }
        b
    }

    pub fn add(&mut self, value: char) {
        if let Some(count) = self.counts.get_mut(value) {
            *count += 1;
        } else {
            self.counts.insert(value, 1);
        }
        self.size += 1;
    }

    /// Removes one occurrence. Returns true if the value was present.
    pub fn remove(&mut self, value: char) -> bool {
        if let Some(count) = self.counts.get_mut(value) {
            *count -= 1;
            if *count == 0 {
                self.counts.remove(value);
            }
            self.size -= 1;
            true
        } else {
            false
        }
    }

    /// Removes all occurrences of the value.
    pub fn remove_all(&mut self, value: char) -> bool {
        if let Some(count) = self.counts.remove(value) {
            self.size -= count;
            true
        } else {
            false
        }
    }

    pub fn occurrences_of(&self, value: char) -> usize {
        self.counts.get(value).unwrap_or(0)
    }

    pub fn contains(&self, value: char) -> bool {
        self.occurrences_of(value) > 0
    }
    pub fn size(&self) -> usize {
        self.size
    }
    pub fn size_distinct(&self) -> usize {
        self.counts.len()
    }
    pub fn is_empty(&self) -> bool {
        self.size == 0
    }

    pub fn clear(&mut self) {
        self.counts.clear();
        self.size = 0;
    }

    /// Ensures the internal count-map can hold `additional` more distinct
    /// values without triggering a rehash. Returns `TryReserveError` on
    /// allocator failure. Note that the count is bounded by distinct
    /// values, not total occurrences. See `docs/rust/error-handling.md`.
    pub fn try_reserve(
        &mut self,
        additional: usize,
    ) -> Result<(), std::collections::TryReserveError> {
        self.counts.try_reserve(additional)
    }

    /// Iterates over all elements, repeating each value by its count.
    pub fn iter(&self) -> impl Iterator<Item = char> + '_ {
        self.counts
            .iter()
            .flat_map(|(k, c)| std::iter::repeat_n(k, c))
    }

    /// Iterates over distinct values.
    pub fn iter_distinct(&self) -> impl Iterator<Item = char> + '_ {
        self.counts.iter().map(|(k, _)| k)
    }

    pub fn for_each_with_occurrences(&self, mut f: impl FnMut(char, usize)) {
        for (v, c) in self.counts.iter() {
            f(v, c);
        }
    }

    pub fn select(&self, predicate: impl Fn(char) -> bool) -> Self {
        let mut result = Self::new();
        self.for_each_with_occurrences(|v, c| {
            if predicate(v) {
                for _ in 0..c {
                    result.add(v);
                }
            }
        });
        result
    }

    pub fn to_vec(&self) -> Vec<char> {
        let mut result = Vec::with_capacity(self.size);
        self.for_each_with_occurrences(|v, c| {
            for _ in 0..c {
                result.push(v);
            }
        });
        result
    }
}

impl Default for CharHashBag {
    fn default() -> Self {
        Self::new()
    }
}

impl fmt::Display for CharHashBag {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        write!(f, "{{")?;
        let mut first = true;
        self.for_each_with_occurrences(|v, c| {
            if !first {
                let _ = write!(f, ", ");
            }
            let _ = write!(f, "{}x{}", v, c);
            first = false;
        });
        write!(f, "}}")
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_add_occurrences() {
        let mut b = CharHashBag::new();
        b.add('a');
        b.add('a');
        b.add('b');
        assert_eq!(b.occurrences_of('a'), 2);
        assert_eq!(b.occurrences_of('b'), 1);
        assert_eq!(b.size(), 3);
        assert_eq!(b.size_distinct(), 2);
    }

    #[test]
    fn test_remove() {
        let mut b = CharHashBag::of(&['a', 'a', 'a']);
        assert!(b.remove('a'));
        assert_eq!(b.occurrences_of('a'), 2);
    }

    #[test]
    fn test_remove_all() {
        let mut b = CharHashBag::of(&['a', 'a', 'b']);
        assert!(b.remove_all('a'));
        assert!(!b.contains('a'));
        assert_eq!(b.size(), 1);
    }

    #[test]
    fn test_clear() {
        let mut b = CharHashBag::of(&['a']);
        b.clear();
        assert!(b.is_empty());
    }

    #[test]
    fn test_for_each_with_occurrences() {
        let b = CharHashBag::of(&['a', 'a', 'b']);
        let mut total = 0;
        b.for_each_with_occurrences(|_, c| total += c);
        assert_eq!(total, 3);
    }

    #[test]
    fn test_select() {
        let b = CharHashBag::of(&['a', 'b', 'c']);
        assert_eq!(b.select(|v| v > 'a').size(), 2);
    }

    #[test]
    fn test_display() {
        let b = CharHashBag::of(&['a']);
        assert!(!b.to_string().is_empty());
    }

    #[test]
    fn test_try_reserve_happy_path() {
        let mut b = CharHashBag::new();
        b.try_reserve(100).unwrap();
    }

    #[test]
    fn test_try_reserve_propagates_capacity_overflow() {
        let mut b = CharHashBag::new();
        assert!(b.try_reserve(usize::MAX / 2).is_err());
    }
}

impl crate::traits::char_collection::CharCollection for CharHashBag {
    fn len(&self) -> usize {
        self.size()
    }
    fn contains(&self, value: char) -> bool {
        self.contains(value)
    }
    fn iter(&self) -> impl Iterator<Item = char> + '_ {
        self.iter()
    }
}

impl crate::traits::char_collection::CharMutableCollection for CharHashBag {
    fn clear(&mut self) {
        self.clear()
    }
}

impl crate::traits::char_collection::CharBag for CharHashBag {
    fn occurrences_of(&self, value: char) -> usize {
        self.occurrences_of(value)
    }
    fn size_distinct(&self) -> usize {
        self.size_distinct()
    }
}

impl crate::traits::char_collection::CharMutableBag for CharHashBag {
    fn add(&mut self, value: char) {
        self.add(value)
    }
}
