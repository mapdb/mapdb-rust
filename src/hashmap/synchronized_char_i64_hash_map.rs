// Code generated by mapdb-codegen. DO NOT EDIT.

use std::sync::RwLock;

use super::char_i64_hash_map::CharI64HashMap;

/// Thread-safe wrapper around [`CharI64HashMap`]. Reads are concurrent;
/// mutations are exclusive. Lock poisoning panics (parity with Go).
#[derive(Debug, Default)]
pub struct SynchronizedCharI64HashMap {
    inner: RwLock<CharI64HashMap>,
}

impl SynchronizedCharI64HashMap {
    pub fn new() -> Self {
        SynchronizedCharI64HashMap {
            inner: RwLock::new(CharI64HashMap::new()),
        }
    }

    pub fn with_capacity(capacity: usize) -> Self {
        SynchronizedCharI64HashMap {
            inner: RwLock::new(CharI64HashMap::with_capacity(capacity)),
        }
    }

    pub fn insert(&self, key: char, value: i64) -> Option<i64> {
        self.inner.write().unwrap().insert(key, value)
    }

    pub fn get(&self, key: char) -> Option<i64> {
        self.inner.read().unwrap().get(key)
    }

    pub fn get_or_default(&self, key: char, default: i64) -> i64 {
        self.inner.read().unwrap().get_or_default(key, default)
    }

    pub fn remove(&self, key: char) -> Option<i64> {
        self.inner.write().unwrap().remove(key)
    }

    pub fn contains_key(&self, key: char) -> bool {
        self.inner.read().unwrap().contains_key(key)
    }

    pub fn contains_value(&self, value: i64) -> bool {
        self.inner.read().unwrap().contains_value(value)
    }

    pub fn len(&self) -> usize {
        self.inner.read().unwrap().len()
    }
    pub fn is_empty(&self) -> bool {
        self.inner.read().unwrap().is_empty()
    }
    pub fn clear(&self) {
        self.inner.write().unwrap().clear();
    }

    pub fn keys_to_vec(&self) -> Vec<char> {
        self.inner.read().unwrap().keys_to_vec()
    }

    pub fn values_to_vec(&self) -> Vec<i64> {
        self.inner.read().unwrap().values_to_vec()
    }

    /// Returns an owned snapshot of entries for iteration under read lock.
    pub fn to_vec(&self) -> Vec<(char, i64)> {
        self.inner.read().unwrap().iter().collect()
    }

    pub fn for_each(&self, mut f: impl FnMut(char, i64)) {
        let snap = self.to_vec();
        for (k, v) in snap {
            f(k, v);
        }
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use std::sync::Arc;
    use std::thread;

    #[test]
    fn test_basic_ops() {
        let m = SynchronizedCharI64HashMap::new();
        assert!(m.is_empty());
        assert_eq!(m.insert('a', 1), None);
        assert_eq!(m.insert('b', 2), None);
        assert_eq!(m.insert('c', 3), None);
        assert_eq!(m.get('a'), Some(1));
        assert!(m.contains_key('a'));
        assert!(m.contains_value(2));
        let old = m.insert('a', 2);
        assert_eq!(old, Some(1));
        assert_eq!(m.remove('a'), Some(2));
        assert!(!m.contains_key('a'));
        m.clear();
        assert!(m.is_empty());
    }

    #[test]
    fn test_get_or_default() {
        let m = SynchronizedCharI64HashMap::new();
        m.insert('a', 1);
        assert_eq!(m.get_or_default('a', 3), 1);
        assert_eq!(m.get_or_default('z', 3), 3);
    }

    #[test]
    fn test_snapshots_and_for_each() {
        let m = SynchronizedCharI64HashMap::new();
        m.insert('a', 1);
        m.insert('b', 2);
        assert_eq!(m.keys_to_vec().len(), 2);
        assert_eq!(m.values_to_vec().len(), 2);
        assert_eq!(m.to_vec().len(), 2);
        let mut count = 0usize;
        m.for_each(|_, _| count += 1);
        assert_eq!(count, 2);
    }

    #[test]
    fn test_concurrent() {
        let m = Arc::new(SynchronizedCharI64HashMap::new());
        let mut handles = Vec::new();
        for _ in 0..4 {
            let c = Arc::clone(&m);
            handles.push(thread::spawn(move || {
                for _ in 0..100 {
                    c.insert('a', 1);
                    c.get('a');
                }
            }));
        }
        for h in handles {
            h.join().unwrap();
        }
        assert_eq!(m.len(), 1);
        assert_eq!(m.get('a'), Some(1));
    }
}
