// Code generated by mapdb-codegen. DO NOT EDIT.

use std::collections::BTreeSet;
use std::fmt;

/// Sorted set of unique `bool` values.
#[derive(Debug, Clone)]
pub struct BoolTreeSet {
    inner: BTreeSet<bool>,
}

impl BoolTreeSet {
    pub fn new() -> Self {
        BoolTreeSet {
            inner: BTreeSet::new(),
        }
    }

    pub fn of(values: &[bool]) -> Self {
        let mut s = Self::new();
        for &v in values {
            s.add(v);
        }
        s
    }

    pub fn add(&mut self, value: bool) -> bool {
        self.inner.insert(value)
    }

    pub fn remove(&mut self, value: bool) -> bool {
        self.inner.remove(&value)
    }

    pub fn contains(&self, value: bool) -> bool {
        self.inner.contains(&value)
    }

    pub fn len(&self) -> usize {
        self.inner.len()
    }
    pub fn is_empty(&self) -> bool {
        self.len() == 0
    }
    pub fn clear(&mut self) {
        self.inner.clear();
    }

    /// Ensures that `additional` more entries can be added without a
    /// reallocation on the backing store. Returns `TryReserveError` on
    /// allocator failure. For `BTreeSet`-backed variants this is a no-op
    /// — see the `try_reserve` doc on the matching TreeMap and
    /// `docs/rust/error-handling.md`.
    pub fn try_reserve(
        &mut self,
        additional: usize,
    ) -> Result<(), std::collections::TryReserveError> {
        let _ = additional;
        Ok(())
    }

    pub fn min(&self) -> Option<bool> {
        self.inner.iter().next().copied()
    }

    pub fn max(&self) -> Option<bool> {
        self.inner.iter().next_back().copied()
    }

    /// Iterates in sorted order.
    pub fn iter(&self) -> impl Iterator<Item = bool> + '_ {
        self.inner.iter().copied()
    }

    pub fn for_each(&self, mut f: impl FnMut(bool)) {
        for v in self.iter() {
            f(v);
        }
    }

    pub fn select(&self, predicate: impl Fn(bool) -> bool) -> Self {
        let mut result = Self::new();
        for v in self.iter() {
            if predicate(v) {
                result.add(v);
            }
        }
        result
    }

    pub fn any_satisfy(&self, predicate: impl Fn(bool) -> bool) -> bool {
        self.iter().any(predicate)
    }

    pub fn all_satisfy(&self, predicate: impl Fn(bool) -> bool) -> bool {
        self.iter().all(predicate)
    }

    pub fn union(&self, other: &Self) -> Self {
        let mut result = self.clone();
        for v in other.iter() {
            result.add(v);
        }
        result
    }

    pub fn intersect(&self, other: &Self) -> Self {
        let mut result = Self::new();
        for v in self.iter() {
            if other.contains(v) {
                result.add(v);
            }
        }
        result
    }

    pub fn difference(&self, other: &Self) -> Self {
        let mut result = Self::new();
        for v in self.iter() {
            if !other.contains(v) {
                result.add(v);
            }
        }
        result
    }

    pub fn to_vec(&self) -> Vec<bool> {
        self.iter().collect()
    }
    pub fn with(mut self, value: bool) -> Self {
        self.add(value);
        self
    }
    pub fn without(mut self, value: bool) -> Self {
        self.remove(value);
        self
    }
}

impl Default for BoolTreeSet {
    fn default() -> Self {
        Self::new()
    }
}

impl PartialEq for BoolTreeSet {
    fn eq(&self, other: &Self) -> bool {
        if self.len() != other.len() {
            return false;
        }
        self.iter().zip(other.iter()).all(|(a, b)| a == b)
    }
}

impl fmt::Display for BoolTreeSet {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        write!(f, "{{")?;
        let mut first = true;
        for v in self.iter() {
            if !first {
                write!(f, ", ")?;
            }
            write!(f, "{}", v)?;
            first = false;
        }
        write!(f, "}}")
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_add_contains() {
        let mut s = BoolTreeSet::new();
        s.add(true);
        s.add(false);
        assert_eq!(s.len(), 2);
        assert!(s.contains(false));
    }

    #[test]
    fn test_duplicate() {
        let mut s = BoolTreeSet::new();
        assert!(s.add(true));
        assert!(!s.add(true));
        assert_eq!(s.len(), 1);
    }

    #[test]
    fn test_sorted_iteration() {
        let s = BoolTreeSet::of(&[true, false]);
        let vals: Vec<_> = s.iter().collect();
        assert_eq!(vals, vec![false, true]);
    }

    #[test]
    fn test_min_max() {
        let s = BoolTreeSet::of(&[true, false]);
        assert_eq!(s.min(), Some(false));
        assert_eq!(s.max(), Some(true));
    }

    #[test]
    fn test_remove() {
        let mut s = BoolTreeSet::of(&[true, false]);
        assert!(s.remove(true));
        assert!(!s.contains(true));
    }

    #[test]
    fn test_union() {
        let a = BoolTreeSet::of(&[true]);
        let b = BoolTreeSet::of(&[false]);
        assert_eq!(a.union(&b).len(), 2);
    }

    #[test]
    fn test_intersect() {
        let a = BoolTreeSet::of(&[true, false]);
        let b = BoolTreeSet::of(&[false]);
        assert_eq!(a.intersect(&b).len(), 1);
    }

    #[test]
    fn test_display() {
        let s = BoolTreeSet::of(&[true]);
        assert!(!s.to_string().is_empty());
    }

    #[test]
    fn test_try_reserve_happy_path() {
        let mut s = BoolTreeSet::new();
        s.try_reserve(100).unwrap();
    }
}

impl crate::traits::bool_collection::BoolCollection for BoolTreeSet {
    fn len(&self) -> usize {
        self.len()
    }
    fn contains(&self, value: bool) -> bool {
        self.contains(value)
    }
    fn iter(&self) -> impl Iterator<Item = bool> + '_ {
        self.iter()
    }
}

impl crate::traits::bool_collection::BoolMutableCollection for BoolTreeSet {
    fn clear(&mut self) {
        self.clear()
    }
}

impl crate::traits::bool_collection::BoolSet for BoolTreeSet {}

impl crate::traits::bool_collection::BoolMutableSet for BoolTreeSet {
    fn add(&mut self, value: bool) -> bool {
        self.add(value)
    }
}
