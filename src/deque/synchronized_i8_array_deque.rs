// Code generated by mapdb-codegen. DO NOT EDIT.

use std::sync::RwLock;

use super::i8_array_deque::I8ArrayDeque;

/// Thread-safe wrapper around [`I8ArrayDeque`].
#[derive(Debug, Default)]
pub struct SynchronizedI8ArrayDeque {
    inner: RwLock<I8ArrayDeque>,
}

impl SynchronizedI8ArrayDeque {
    pub fn new() -> Self {
        SynchronizedI8ArrayDeque {
            inner: RwLock::new(I8ArrayDeque::new()),
        }
    }

    pub fn of(values: &[i8]) -> Self {
        SynchronizedI8ArrayDeque {
            inner: RwLock::new(I8ArrayDeque::of(values)),
        }
    }

    pub fn add_first(&self, value: i8) {
        self.inner.write().unwrap().add_first(value);
    }

    pub fn add_last(&self, value: i8) {
        self.inner.write().unwrap().add_last(value);
    }

    pub fn remove_first(&self) -> Option<i8> {
        self.inner.write().unwrap().remove_first()
    }

    pub fn remove_last(&self) -> Option<i8> {
        self.inner.write().unwrap().remove_last()
    }

    pub fn peek_first(&self) -> Option<i8> {
        self.inner.read().unwrap().peek_first()
    }

    pub fn peek_last(&self) -> Option<i8> {
        self.inner.read().unwrap().peek_last()
    }

    pub fn contains(&self, value: i8) -> bool {
        self.inner.read().unwrap().contains(value)
    }

    pub fn len(&self) -> usize {
        self.inner.read().unwrap().len()
    }
    pub fn is_empty(&self) -> bool {
        self.inner.read().unwrap().is_empty()
    }
    pub fn clear(&self) {
        self.inner.write().unwrap().clear();
    }

    pub fn to_vec(&self) -> Vec<i8> {
        self.inner.read().unwrap().to_vec()
    }

    pub fn for_each(&self, f: impl FnMut(i8)) {
        self.inner.read().unwrap().for_each(f);
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use std::sync::Arc;
    use std::thread;

    #[test]
    fn test_basic_ops() {
        let d = SynchronizedI8ArrayDeque::new();
        assert!(d.is_empty());
        d.add_last(1);
        d.add_last(2);
        d.add_first(3);
        assert_eq!(d.len(), 3);
        assert_eq!(d.peek_first(), Some(3));
        assert_eq!(d.peek_last(), Some(2));
        assert_eq!(d.remove_first(), Some(3));
        assert_eq!(d.remove_last(), Some(2));
        assert!(d.contains(1));
        d.clear();
        assert!(d.is_empty());
    }

    #[test]
    fn test_to_vec_and_for_each() {
        let d = SynchronizedI8ArrayDeque::of(&[1, 2, 3]);
        assert_eq!(d.to_vec().len(), 3);
        let mut count = 0usize;
        d.for_each(|_| count += 1);
        assert_eq!(count, 3);
    }

    #[test]
    fn test_concurrent() {
        let d = Arc::new(SynchronizedI8ArrayDeque::new());
        let mut handles = Vec::new();
        for _ in 0..4 {
            let c = Arc::clone(&d);
            handles.push(thread::spawn(move || {
                for _ in 0..100 {
                    c.add_last(1);
                }
            }));
        }
        for h in handles {
            h.join().unwrap();
        }
        assert_eq!(d.len(), 400);
    }
}
