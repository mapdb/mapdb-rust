// Code generated by mapdb-codegen. DO NOT EDIT.
use crate::hashmap::bool_bool_hash_map::BoolBoolHashMap;
use std::collections::HashMap;
use std::fmt;

/// Immutable hash map from `bool` keys to `bool` values. Clone is cheap (shared data).
#[derive(Debug, Clone)]
pub struct ImmutableBoolBoolHashMap {
    inner: HashMap<bool, bool>,
}

impl ImmutableBoolBoolHashMap {
    pub fn from_mutable(m: &BoolBoolHashMap) -> Self {
        let mut inner = HashMap::new();
        m.for_each(|k, v| {
            inner.insert(k, v);
        });
        ImmutableBoolBoolHashMap { inner }
    }
    pub fn get(&self, key: bool) -> Option<bool> {
        self.inner.get(&(key)).copied()
    }
    pub fn get_or_default(&self, key: bool, default: bool) -> bool {
        self.get(key).unwrap_or(default)
    }
    pub fn contains_key(&self, key: bool) -> bool {
        self.inner.contains_key(&(key))
    }
    pub fn len(&self) -> usize {
        self.inner.len()
    }
    pub fn is_empty(&self) -> bool {
        self.inner.is_empty()
    }
    pub fn iter(&self) -> impl Iterator<Item = (bool, bool)> + '_ {
        self.inner.iter().map(|(k, &v)| (*k, v))
    }
    pub fn keys(&self) -> impl Iterator<Item = bool> + '_ {
        self.inner.keys().copied()
    }
    pub fn values(&self) -> impl Iterator<Item = bool> + '_ {
        self.inner.values().copied()
    }
    pub fn for_each(&self, mut f: impl FnMut(bool, bool)) {
        for (k, v) in self.iter() {
            f(k, v);
        }
    }
    pub fn select(&self, predicate: impl Fn(bool, bool) -> bool) -> Self {
        let mut inner = HashMap::new();
        for (k, v) in self.iter() {
            if predicate(k, v) {
                inner.insert(k, v);
            }
        }
        ImmutableBoolBoolHashMap { inner }
    }
    pub fn any_satisfy(&self, predicate: impl Fn(bool, bool) -> bool) -> bool {
        self.iter().any(|(k, v)| predicate(k, v))
    }
    pub fn all_satisfy(&self, predicate: impl Fn(bool, bool) -> bool) -> bool {
        self.iter().all(|(k, v)| predicate(k, v))
    }
    pub fn to_mutable(&self) -> BoolBoolHashMap {
        let mut m = BoolBoolHashMap::new();
        for (k, v) in self.iter() {
            m.insert(k, v);
        }
        m
    }
}
impl PartialEq for ImmutableBoolBoolHashMap {
    fn eq(&self, other: &Self) -> bool {
        if self.len() != other.len() {
            return false;
        }
        self.iter()
            .all(|(k, v)| other.get(k).is_some_and(|ov| v == ov))
    }
}
impl fmt::Display for ImmutableBoolBoolHashMap {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        write!(f, "{{")?;
        let mut first = true;
        for (k, v) in self.iter() {
            if !first {
                write!(f, ", ")?;
            }
            write!(f, "{}={}", k, v)?;
            first = false;
        }
        write!(f, "}}")
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn test_get() {
        let mut m = BoolBoolHashMap::new();
        m.insert(true, true);
        m.insert(false, false);
        let im = ImmutableBoolBoolHashMap::from_mutable(&m);
        assert_eq!(im.get(true), Some(true));
        assert_eq!(im.len(), 2);
    }
    #[test]
    fn test_contains_key() {
        let mut m = BoolBoolHashMap::new();
        m.insert(true, true);
        let im = ImmutableBoolBoolHashMap::from_mutable(&m);
        assert!(im.contains_key(true));
    }
    #[test]
    fn test_to_mutable_independent() {
        let mut m = BoolBoolHashMap::new();
        m.insert(true, true);
        let im = ImmutableBoolBoolHashMap::from_mutable(&m);
        let mut m2 = im.to_mutable();
        m2.insert(false, false);
        assert_eq!(im.len(), 1);
    }
    #[test]
    fn test_display() {
        let mut m = BoolBoolHashMap::new();
        m.insert(true, true);
        assert!(!ImmutableBoolBoolHashMap::from_mutable(&m)
            .to_string()
            .is_empty());
    }
}

impl crate::traits::bool_bool_map::BoolBoolMap for ImmutableBoolBoolHashMap {
    fn len(&self) -> usize {
        self.len()
    }
    fn contains_key(&self, key: bool) -> bool {
        self.contains_key(key)
    }
    fn get(&self, key: bool) -> Option<bool> {
        self.get(key)
    }
    fn iter(&self) -> impl Iterator<Item = (bool, bool)> + '_ {
        self.iter()
    }
}
