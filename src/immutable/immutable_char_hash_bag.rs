// Code generated by mapdb-codegen. DO NOT EDIT.
use crate::bag::char_hash_bag::CharHashBag;
use std::collections::HashMap;
use std::fmt;

/// Immutable bag (multiset) of `char` values with occurrence counts.
#[derive(Debug, Clone)]
pub struct ImmutableCharHashBag {
    counts: HashMap<char, usize>,
    size: usize,
}

impl ImmutableCharHashBag {
    pub fn from_mutable(bag: &CharHashBag) -> Self {
        let mut im = ImmutableCharHashBag {
            counts: HashMap::new(),
            size: 0,
        };
        bag.for_each_with_occurrences(|v, c| {
            im.counts.insert(v, c);
            im.size += c;
        });
        im
    }
    pub fn of(values: &[char]) -> Self {
        let m = CharHashBag::of(values);
        Self::from_mutable(&m)
    }
    pub fn occurrences_of(&self, value: char) -> usize {
        self.counts.get(&value).copied().unwrap_or(0)
    }
    pub fn contains(&self, value: char) -> bool {
        self.occurrences_of(value) > 0
    }
    pub fn size(&self) -> usize {
        self.size
    }
    pub fn size_distinct(&self) -> usize {
        self.counts.len()
    }
    pub fn is_empty(&self) -> bool {
        self.size == 0
    }
    /// Iterates over all elements, repeating each value by its count.
    pub fn iter(&self) -> impl Iterator<Item = char> + '_ {
        self.counts
            .iter()
            .flat_map(|(&v, &c)| std::iter::repeat_n(v, c))
    }
    pub fn to_mutable(&self) -> CharHashBag {
        let mut m = CharHashBag::new();
        for (&v, &c) in &self.counts {
            for _ in 0..c {
                m.add(v);
            }
        }
        m
    }
    /// Expands the bag to a `Vec` containing each value repeated by its count.
    pub fn to_vec(&self) -> Vec<char> {
        let mut out = Vec::with_capacity(self.size);
        for (&v, &c) in &self.counts {
            for _ in 0..c {
                out.push(v);
            }
        }
        out
    }
}

impl crate::traits::char_collection::CharCollection for ImmutableCharHashBag {
    fn len(&self) -> usize {
        self.size()
    }
    fn contains(&self, value: char) -> bool {
        self.contains(value)
    }
    fn iter(&self) -> impl Iterator<Item = char> + '_ {
        self.iter()
    }
}

impl crate::traits::char_collection::CharBag for ImmutableCharHashBag {
    fn occurrences_of(&self, value: char) -> usize {
        self.occurrences_of(value)
    }
    fn size_distinct(&self) -> usize {
        self.size_distinct()
    }
}

impl fmt::Display for ImmutableCharHashBag {
    fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
        write!(f, "ImmutableBag(size={})", self.size)
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn test_occurrences() {
        let im = ImmutableCharHashBag::of(&['a', 'a', 'b']);
        assert_eq!(im.occurrences_of('a'), 2);
        assert_eq!(im.size(), 3);
    }
    #[test]
    fn test_to_mutable() {
        let im = ImmutableCharHashBag::of(&['a']);
        let mut m = im.to_mutable();
        m.add('b');
        assert_eq!(im.size(), 1);
    }
    #[test]
    fn test_display() {
        assert!(!ImmutableCharHashBag::of(&['a']).to_string().is_empty());
    }
}
