// Code generated by mapdb-codegen. DO NOT EDIT.

/// Read-only trait for any map from `bool` keys to `char` values.
///
/// Implementors provide four required methods — [`len`], [`contains_key`],
/// [`get`], and [`iter`] — and get a rich set of defaulted query methods
/// for free, following the same pattern as the Collection trait.
pub trait BoolCharMap {
    // ── Required methods ────────────────────────────────────────────

    /// Returns the number of key-value entries.
    fn len(&self) -> usize;

    /// Returns true if the map contains an entry for the key.
    fn contains_key(&self, key: bool) -> bool;

    /// Returns the value for the key, or None if absent.
    fn get(&self, key: bool) -> Option<char>;

    /// Returns an iterator over (key, value) pairs.
    fn iter(&self) -> impl Iterator<Item = (bool, char)> + '_;

    // ── Defaulted methods ───────────────────────────────────────────

    /// Returns true if the map is empty.
    fn is_empty(&self) -> bool {
        self.len() == 0
    }

    /// Returns true if the map contains an entry with the given value.
    fn contains_value(&self, value: char) -> bool {
        self.iter().any(|(_, v)| v == value)
    }

    /// Calls the function for each key-value pair.
    fn for_each_key_value(&self, mut f: impl FnMut(bool, char)) {
        for (k, v) in self.iter() {
            f(k, v);
        }
    }

    /// Returns true if any entry satisfies the predicate.
    fn any_satisfy(&self, predicate: impl Fn(bool, char) -> bool) -> bool {
        self.iter().any(|(k, v)| predicate(k, v))
    }

    /// Returns true if all entries satisfy the predicate.
    fn all_satisfy(&self, predicate: impl Fn(bool, char) -> bool) -> bool {
        self.iter().all(|(k, v)| predicate(k, v))
    }

    /// Returns true if no entry satisfies the predicate.
    fn none_satisfy(&self, predicate: impl Fn(bool, char) -> bool) -> bool {
        !self.iter().any(|(k, v)| predicate(k, v))
    }

    /// Returns the count of entries satisfying the predicate.
    fn count_where(&self, predicate: impl Fn(bool, char) -> bool) -> usize {
        self.iter().filter(|&(k, v)| predicate(k, v)).count()
    }

    /// Returns the first entry satisfying the predicate, or None.
    fn detect(&self, predicate: impl Fn(bool, char) -> bool) -> Option<(bool, char)> {
        self.iter().find(|&(k, v)| predicate(k, v))
    }

    /// Folds all entries using the given function and initial value.
    fn inject_into<R>(&self, initial: R, mut f: impl FnMut(R, bool, char) -> R) -> R {
        let mut acc = initial;
        for (k, v) in self.iter() {
            acc = f(acc, k, v);
        }
        acc
    }

    /// Returns all keys as a Vec.
    fn keys_to_vec(&self) -> Vec<bool> {
        self.iter().map(|(k, _)| k).collect()
    }

    /// Returns all values as a Vec.
    fn values_to_vec(&self) -> Vec<char> {
        self.iter().map(|(_, v)| v).collect()
    }
}

/// Mutable map trait extending BoolCharMap.
pub trait BoolCharMutableMap: BoolCharMap {
    /// Inserts a key/value pair. Returns the previous value if present.
    fn insert(&mut self, key: bool, value: char) -> Option<char>;

    /// Removes the entry for the key. Returns the removed value if present.
    fn remove(&mut self, key: bool) -> Option<char>;

    /// Removes all entries.
    fn clear(&mut self);
}

#[cfg(test)]
mod verify {
    use super::*;
    fn _assert_map<T: BoolCharMap>() {}
    fn _assert_mut<T: BoolCharMutableMap>() {}

    /// Compile-time verification: every concrete `bool` → `char` map type
    /// implements the read-only and (where applicable) mutable map trait.
    #[test]
    fn types_implement_traits() {
        _assert_map::<crate::hashmap::bool_char_hash_map::BoolCharHashMap>();
        _assert_mut::<crate::hashmap::bool_char_hash_map::BoolCharHashMap>();
        _assert_map::<crate::treemap::bool_char_tree_map::BoolCharTreeMap>();
        _assert_mut::<crate::treemap::bool_char_tree_map::BoolCharTreeMap>();
        _assert_map::<crate::hashmap::bool_char_hash_bi_map::BoolCharHashBiMap>();
        _assert_mut::<crate::hashmap::bool_char_hash_bi_map::BoolCharHashBiMap>();
        _assert_map::<crate::immutable::immutable_bool_char_hash_map::ImmutableBoolCharHashMap>();
    }
}
