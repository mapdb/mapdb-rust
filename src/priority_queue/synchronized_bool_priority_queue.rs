// Code generated by mapdb-codegen. DO NOT EDIT.

use std::sync::RwLock;

use super::bool_priority_queue::BoolPriorityQueue;

/// Thread-safe wrapper around [`BoolPriorityQueue`].
#[derive(Debug, Default)]
pub struct SynchronizedBoolPriorityQueue {
    inner: RwLock<BoolPriorityQueue>,
}

impl SynchronizedBoolPriorityQueue {
    pub fn new() -> Self {
        SynchronizedBoolPriorityQueue {
            inner: RwLock::new(BoolPriorityQueue::new()),
        }
    }

    pub fn of(values: &[bool]) -> Self {
        SynchronizedBoolPriorityQueue {
            inner: RwLock::new(BoolPriorityQueue::of(values)),
        }
    }

    pub fn push(&self, value: bool) {
        self.inner.write().unwrap().push(value);
    }

    pub fn pop(&self) -> Option<bool> {
        self.inner.write().unwrap().pop()
    }

    pub fn peek(&self) -> Option<bool> {
        self.inner.read().unwrap().peek()
    }

    pub fn contains(&self, value: bool) -> bool {
        self.inner.read().unwrap().contains(value)
    }

    pub fn len(&self) -> usize {
        self.inner.read().unwrap().len()
    }
    pub fn is_empty(&self) -> bool {
        self.inner.read().unwrap().is_empty()
    }
    pub fn clear(&self) {
        self.inner.write().unwrap().clear();
    }

    pub fn to_vec(&self) -> Vec<bool> {
        self.inner.read().unwrap().to_vec()
    }

    pub fn for_each(&self, mut f: impl FnMut(bool)) {
        let snap = self.inner.read().unwrap().to_vec();
        for v in snap {
            f(v);
        }
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use std::sync::Arc;
    use std::thread;

    #[test]
    fn test_basic_ops() {
        let q = SynchronizedBoolPriorityQueue::new();
        assert!(q.is_empty());
        q.push(true);
        q.push(true);
        q.push(false);
        assert_eq!(q.len(), 3);
        assert!(q.peek().is_some());
        assert!(q.pop().is_some());
        assert_eq!(q.len(), 2);
        q.clear();
        assert!(q.is_empty());
    }

    #[test]
    fn test_to_vec_and_for_each() {
        let q = SynchronizedBoolPriorityQueue::of(&[true, false, true]);
        assert_eq!(q.to_vec().len(), 3);
        let mut count = 0usize;
        q.for_each(|_| count += 1);
        assert_eq!(count, 3);
    }

    #[test]
    fn test_concurrent() {
        let q = Arc::new(SynchronizedBoolPriorityQueue::new());
        let mut handles = Vec::new();
        for _ in 0..4 {
            let c = Arc::clone(&q);
            handles.push(thread::spawn(move || {
                for _ in 0..100 {
                    c.push(true);
                }
            }));
        }
        for h in handles {
            h.join().unwrap();
        }
        assert_eq!(q.len(), 400);
    }
}
